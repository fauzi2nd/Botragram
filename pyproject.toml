[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "botragram"
version = "1.4.0"
description = "Asynchronous algorithmic trading framework."
requires-python = ">=3.14"

dependencies = [
    "aiohttp",
    "aiosqlite",
    "python-dotenv",
    "python-telegram-bot",
    "rich==15.0.0",
]

[project.optional-dependencies]
dev = [
    "mypy",
    "pyright",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "ruff",
]

[tool.pyright]
pythonVersion = "3.14"
typeCheckingMode = "strict"
include = ["botragram", "tests", "main.py"]
venvPath = "."
venv = ".venv"

[tool.mypy]
python_version = "3.14"
strict = true
files = ["botragram", "tests", "main.py"]
exclude = [".venv"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
norecursedirs = ["manual"]
addopts = "--basetemp=.pytest-runtime"
cache_dir = ".cache/pytest-runtime/cache"

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = []
