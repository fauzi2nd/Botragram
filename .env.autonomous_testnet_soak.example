# =============================================================================
# Dedicated autonomous FUTURES TESTNET soak configuration
# =============================================================================
# Copy to .env.autonomous_testnet_soak. Select it explicitly with:
# $env:BOTRAGRAM_ENV_FILE = '.env.autonomous_testnet_soak'
#
# Its isolated TESTNET credentials belong only in the sibling
# .env.autonomous_testnet_soak.testnet file. Never put MAINNET credentials here.

BOTRAGRAM_PROFILE=TESTNET
ACTIVE_EXCHANGE=BINANCE

# Telegram monitoring for the soak operator.
TELEGRAM_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Explicit autonomous LIVE TESTNET capability envelope.
TRADE_MODE=LIVE
EXECUTION_POLICY=autonomous_live
STRATEGY_TYPE=ema_cross
AUTONOMOUS_EXECUTION_ENABLED=false
AUTONOMOUS_LIVE_ENTRY_ENABLED=true
AUTONOMOUS_MAINNET_ENTRY_ENABLED=false
BINANCE_MARKET_TYPE=FUTURES
MARKET_INTERVAL=1m

# Conservative TESTNET runtime values and immutable Telegram ceilings.
# /setrisklimits may change durable runtime values only <= these values while PAUSED.
MAX_POSITION_SIZE_USDT=10
MAX_OPEN_POSITIONS=1
RISK_PER_TRADE_PCT=0.02
MAX_DRAWDOWN_PCT=0.10
LEVERAGE=1
MAX_EXECUTABLE_QUOTE_AGE_MS=1000
MAX_SPREAD_BPS=20

# Strategy-specific protection used by this soak profile.
EMA_SCALPING_STOP_LOSS_PCT=0.005
EMA_SCALPING_TAKE_PROFIT_PCT=0.01
EMA_CROSS_STOP_LOSS_PCT=0.001
EMA_CROSS_TAKE_PROFIT_PCT=0.0015

# Retained for compatibility/telemetry; autonomous LIVE still rotates bounded
# batches across the complete active ranked Futures universe.
DISCOVERY_UNIVERSE_LIMIT=100
DISCOVERY_BATCH_SIZE=20
DISCOVERY_CADENCE_SECONDS=10

LOG_LEVEL=INFO
